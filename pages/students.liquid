---
order: 3
---

{{ text.students.intro | markdown }}
{% for cohort in students %}
	<section>
		<h2>{{ cohort.year }}</h2>
		{% render 'people.liquid', people: cohort.students %}
	</section>
{% endfor %}

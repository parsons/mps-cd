<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta
			content="initial-scale=1, width=device-width, viewport-fit=cover"
			name="viewport"
		>
		<title>{{ title }}</title>
	</head>
	<body>
		{% include 'nav.liquid' %}
		<main>
			{{ content }}
		</main>
	</body>
</html>

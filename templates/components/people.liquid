<ul>
	{% assign sorted = people | sort_natural: 'last' %}
	{% for person in sorted %}
		<li>
			<address>
				<a {%- if person.url %} href="{{ person.url }}" {% endif %}>
					{{- person.first }} {{ person.last -}}
				</a>
			</address>
		</li>
	{% endfor %}
</ul>

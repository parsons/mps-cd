# MPS CD
Rebuild of the Parsons MPS CD program website.

## Setup

You’ll need [`pnpm` installed](https://pnpm.io/installation) on your machine. Then run `pnpm install` once to grab our dependencies.

For development, `pnpm run serve` will build/serve the site at `http://localhost/`.
